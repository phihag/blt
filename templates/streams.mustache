<!DOCTYPE html>
<html>
<head>
<meta charset="utf-8" />
<title>Video-Streams</title>
<meta name="viewport" content="width=device-width"/>
<link rel="stylesheet" href="{{static_path}}/native.css" />
<style>
h1 {margin-top: 0.2em; text-align: center;}
td {line-height: 1.4em;}
</style>
</head>
<body>

<h1>Video-Stream-URLs</h1>

<p>Die folgenden URLs können in OBS oder ähnlicher Streaming-Software als Browser-Source eingebunden werden:</p>

<table>
{{#teams}}
<tr id="{{short_name_lower}}">
	<td style="text-align:center;padding:10px 0;vertical-align:middle;width:4em;" rowspan="4"><img style="vertical-align:middle;height:2em;max-width:4em;" src="{{static_path}}{{logo}}" /></td>
	<td style="vertical-align:middle;" rowspan="4">{{team_name}}</td>
	<td style="text-align:right;">2 Courts</td>
	<td><a href="/s/{{short_name_lower}}">{{bbt_base_url}}/s/<strong>{{short_name_lower}}</strong></a></td>
</tr>
{{#courts}}
<tr>
	<td style="text-align:right; vertical-align: top">Court {{court_id}}</td>
	<td><a href="/s/{{short_name_lower}}/{{court_id}}">{{bbt_base_url}}/s/{{short_name_lower}}/{{court_id}}</a> (oder <a href="/s/{{short_name_lower}}/{{court_id}}?dm_style=stripes">stripes-Anzeige</a>)</td>
</tr>
{{/courts}}
<tr>
	<td style="text-align:right; vertical-align: top;">Team Score</td>
	<td><a href="/s/{{short_name_lower}}/team">{{bbt_base_url}}/s/{{short_name_lower}}/team</a></td>
</tr>
<tr><td colspan="4" style="padding-bottom: 1em;"></td></tr>
{{/teams}}
</table>

</body>
</html>